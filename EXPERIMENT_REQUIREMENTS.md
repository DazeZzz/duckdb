# 第五章实验数据需求文档

## 实验环境配置

### 1. 硬件环境
- CPU型号、核心数、主频
- 内存大小和类型
- 存储设备类型（SSD型号、读写速度）
- 操作系统版本

### 2. 软件配置
- DuckDB版本（原生版本 vs 修改后版本）
- 编译器版本和编译选项
- 使用的基准测试：TPC-H（建议使用SF=10或SF=100）
- 查询集合：建议包含TPC-H的22个查询

### 3. 评价指标定义
- **响应时间（Response Time）**：查询从提交到完成的总时间（秒）
- **吞吐量（Throughput）**：单位时间内完成的查询数量（queries/second）
- **Slowdown**：查询实际执行时间 / 理想执行时间（单独运行时的时间）
- **P50/P90/P99延迟**：第50/90/99百分位的查询响应时间
- **调度开销**：调度器本身消耗的CPU时间占比（%）
- **并行效率**：实际加速比 / 理想加速比（%）

---

## 实验1：阶段感知调度的性能评估

### 1.1 Inelastic-First策略的有效性

**实验设置：**
- 混合负载：同时运行多个TPC-H查询（建议：2个短查询 + 2个长查询）
- 短查询：Q1, Q6（扫描密集型）
- 长查询：Q9, Q21（连接密集型）
- 并发度：4个查询同时提交

**需要的数据：**
1. **原生DuckDB（FIFO调度）**：
   - 每个查询的响应时间（秒）
   - 每个查询的slowdown值
   - 平均响应时间
   - 平均slowdown

2. **阶段感知调度器（IF策略）**：
   - 每个查询的响应时间（秒）
   - 每个查询的slowdown值
   - 平均响应时间
   - 平均slowdown
   - 性能改善百分比

3. **阶段识别统计**：
   - 各查询中弹性Task的数量和占比
   - 各查询中非弹性Task的数量和占比
   - 静态分析准确率（%）
   - 动态调整次数

**数据格式示例：**
```
Query,Native_Time(s),Native_Slowdown,IF_Time(s),IF_Slowdown,Improvement(%)
Q1,12.5,2.1,8.3,1.4,33.6
Q6,5.2,1.8,3.1,1.1,40.4
Q9,45.3,3.2,32.1,2.3,29.1
Q21,67.8,4.5,48.2,3.2,28.9
Average,32.7,2.9,23.0,2.0,33.0
```

### 1.2 不同负载下的响应时间分析

**实验设置：**
- 测试3种负载模式：
  1. 低并发（2个查询）
  2. 中并发（4个查询）
  3. 高并发（8个查询）
- 每种模式运行5次，取平均值

**需要的数据：**
- 不同并发度下的平均响应时间（原生 vs IF策略）
- 不同并发度下的P99延迟
- 不同并发度下的吞吐量（queries/second）

**数据格式示例：**
```
Concurrency,Native_AvgTime(s),IF_AvgTime(s),Native_P99(s),IF_P99(s),Native_Throughput,IF_Throughput
2,15.2,12.3,18.5,14.2,0.13,0.16
4,32.7,23.0,45.3,32.1,0.12,0.17
8,68.5,42.1,95.2,58.3,0.12,0.19
```

### 1.3 与轮询调度的对比

**实验设置：**
- 实现Round-Robin调度作为对比基准
- 混合负载：4个查询并发

**需要的数据：**
- FIFO、Round-Robin、IF策略的平均响应时间对比
- 三种策略的公平性指标（slowdown的标准差）

---

## 实验2：自适应任务粒度控制的效果

### 2.1 任务执行时间稳定性

**实验设置：**
- 选择3个代表性查询：Q1（扫描）、Q9（连接）、Q18（聚合）
- 对比固定Morsel（10万行）vs 自适应Morsel

**需要的数据：**
1. **固定Morsel（10万行）**：
   - 每个查询的Task执行时间分布（最小值、最大值、平均值、标准差）
   - Task执行时间的直方图数据（分桶：0-1ms, 1-2ms, 2-5ms, 5-10ms, 10-50ms, 50-100ms, >100ms）

2. **自适应Morsel**：
   - 每个查询的Task执行时间分布
   - Task执行时间的直方图数据
   - Morsel大小的变化轨迹（前100个Task的Morsel大小）
   - 状态机状态转换次数（GROWING→STABLE, STABLE→SHRINKING等）

**数据格式示例：**
```
Query,Mode,Min(ms),Max(ms),Avg(ms),StdDev(ms),Target_Hit_Rate(%)
Q1,Fixed,1.2,8.5,3.2,1.8,45
Q1,Adaptive,1.8,2.3,2.0,0.2,92
Q9,Fixed,5.3,180.2,52.3,35.6,12
Q9,Adaptive,1.7,2.5,2.1,0.3,88
```

### 2.2 调度开销分析

**实验设置：**
- 测量不同Morsel大小下的调度开销
- Morsel大小：1万、5万、10万、20万、50万行

**需要的数据：**
- 每种Morsel大小下的：
  - 总Task数量
  - 调度总耗时（微秒）
  - 调度开销占比（%）
  - 查询总执行时间（秒）

**数据格式示例：**
```
Morsel_Size,Total_Tasks,Schedule_Time(us),Schedule_Overhead(%),Query_Time(s)
10000,5000,5200,0.52,10.2
50000,1000,1050,0.11,9.8
100000,500,520,0.05,9.5
200000,250,265,0.03,9.6
500000,100,105,0.01,10.1
Adaptive,480,500,0.05,9.3
```

### 2.3 Photo Finish机制的作用

**实验设置：**
- 选择数据量不能被Morsel整除的查询
- 对比有/无Photo Finish机制

**需要的数据：**
- 最后5个Task的执行时间（有/无Photo Finish）
- Pipeline尾延迟（最后一个Task的执行时间）
- 尾延迟改善百分比

**数据格式示例：**
```
Query,Without_PF_Last5_Tasks(ms),With_PF_Last5_Tasks(ms),Tail_Latency_Reduction(%)
Q1,"2.1,2.0,1.9,1.8,85.3","2.1,2.0,2.2,1.9,2.0",95.2
Q9,"2.3,2.1,2.4,2.2,120.5","2.2,2.3,2.1,2.4,2.3",94.8
```

---

## 实验3：自适应优先级调整的影响

### 3.1 吞吐量跟踪的准确性

**实验设置：**
- 运行长查询，记录吞吐量跟踪数据
- 采样间隔：1秒

**需要的数据：**
- 查询执行过程中的吞吐量变化（每秒完成的Task数）
- 实际吞吐量 vs 估算吞吐量的对比
- 估算误差（%）

### 3.2 优先级动态调整的效果

**实验设置：**
- 混合负载：1个长查询 + 多个短查询陆续提交
- 记录优先级变化

**需要的数据：**
- 长查询的优先级变化轨迹（时间 vs 优先级）
- 短查询的平均响应时间（有/无优先级调整）
- 长查询的完成时间（有/无优先级调整）

### 3.3 公平性保证验证

**实验设置：**
- 运行多个相同查询，测试公平性
- 5个相同的Q9查询同时提交

**需要的数据：**
- 每个查询的完成时间
- Slowdown的标准差（衡量公平性）
- 最大slowdown / 最小slowdown的比值

---

## 实验4：无锁调度器的扩展性

### 4.1 多核扩展性测试

**实验设置：**
- 测试不同核心数：2, 4, 8, 16核
- 固定负载：8个查询并发

**需要的数据：**
- 不同核心数下的吞吐量（queries/second）
- 不同核心数下的平均响应时间
- 扩展性曲线（加速比 vs 核心数）

**数据格式示例：**
```
Cores,Lock_Based_Throughput,LockFree_Throughput,Lock_Based_Time(s),LockFree_Time(s),Speedup
2,0.08,0.09,25.0,22.2,1.13
4,0.12,0.17,33.3,23.5,1.42
8,0.12,0.19,33.3,21.1,1.58
16,0.11,0.20,36.4,20.0,1.82
```

### 4.2 与基于锁实现的对比

**实验设置：**
- 高并发场景：16个查询并发
- 对比基于锁 vs 无锁实现

**需要的数据：**
- 调度器锁竞争次数
- 平均锁等待时间（微秒）
- 调度吞吐量（schedules/second）

### 4.3 线程竞争分析

**实验设置：**
- 使用性能分析工具（如perf）测量锁竞争

**需要的数据：**
- 锁竞争热点函数
- CAS操作成功率（%）
- 重试次数分布

---

## 实验5：自调优框架的自适应能力

### 5.1 参数自动调整的效果

**实验设置：**
- 运行动态变化的负载
- 记录参数调整过程

**需要的数据：**
- 关键参数的变化轨迹（如优先级衰减系数、Morsel目标时间）
- 手动调优 vs 自动调优的性能对比
- 参数收敛时间

### 5.2 不同负载模式下的适应性

**实验设置：**
- 测试3种负载模式切换：
  1. 短查询密集（0-5分钟）
  2. 长查询密集（5-10分钟）
  3. 混合负载（10-15分钟）

**需要的数据：**
- 每个阶段的平均响应时间
- 负载切换时的适应时间（秒）
- 自调优 vs 静态配置的性能对比

### 5.3 调优开销分析

**需要的数据：**
- 调优线程的CPU占用率（%）
- 仿真评估的耗时（毫秒）
- 调优频率（次/分钟）

---

## 实验6：整体系统性能对比

### 6.1 与原生DuckDB的对比

**实验设置：**
- TPC-H全部22个查询
- 3种场景：
  1. 单查询顺序执行
  2. 低并发（2个查询）
  3. 高并发（8个查询）

**需要的数据：**
- 每个查询在3种场景下的响应时间（原生 vs 优化后）
- 总执行时间对比
- 性能改善百分比

**数据格式示例：**
```
Query,Native_Sequential(s),Optimized_Sequential(s),Native_Concurrent(s),Optimized_Concurrent(s),Improvement(%)
Q1,5.2,5.1,12.5,8.3,33.6
Q2,3.8,3.7,8.9,6.2,30.3
...
Q22,4.1,4.0,9.8,6.5,33.7
Total,125.3,123.5,456.7,312.4,31.6
```

### 6.2 混合负载下的综合性能

**实验设置：**
- 模拟真实场景：短查询持续提交 + 后台长查询
- 运行时间：10分钟
- 短查询提交间隔：5秒

**需要的数据：**
- 短查询的平均响应时间、P99延迟
- 长查询的完成时间
- 系统吞吐量（queries/minute）
- CPU利用率（%）

### 6.3 可预测性改善

**实验设置：**
- 同一查询运行10次，测量响应时间的变异性

**需要的数据：**
- 每个查询的响应时间标准差（原生 vs 优化后）
- 变异系数（CV = 标准差/平均值）
- 可预测性改善百分比

---

## 数据收集建议

### 1. 日志格式
建议在DuckDB中添加日志输出，记录：
- 每个Task的开始/结束时间、执行时间、Morsel大小、阶段类型
- 每次调度决策的时间戳、选中的查询ID、Task ID
- 优先级变化事件
- 状态机转换事件

### 2. 性能计数器
建议添加性能计数器：
- 调度次数、调度总耗时
- 锁竞争次数、CAS重试次数
- 阶段识别准确率
- 参数调整次数

### 3. 输出格式
建议输出CSV格式，便于后续处理和绘图：
```csv
timestamp,query_id,task_id,event_type,duration_ms,morsel_size,phase,priority
1678901234.123,Q1,T001,task_start,0,100000,ELASTIC,5
1678901234.125,Q1,T001,task_end,2.1,100000,ELASTIC,5
```

### 4. 实验重复性
- 每个实验至少运行5次，报告平均值和标准差
- 使用相同的随机种子确保可重复性
- 清空缓存后再运行每次实验

---

## 预期图表

根据实验数据，第五章需要绘制以下图表：

1. **柱状图**：不同调度策略的平均响应时间对比
2. **折线图**：不同并发度下的性能变化
3. **直方图**：Task执行时间分布
4. **时间序列图**：Morsel大小变化轨迹、优先级变化轨迹
5. **散点图**：实际吞吐量 vs 估算吞吐量
6. **扩展性曲线**：加速比 vs 核心数
7. **热力图**：不同查询在不同负载下的性能矩阵
8. **箱线图**：响应时间的分布和离群值

---

## 总结

以上是第五章所需的全部实验数据要求。关键数据包括：

1. **性能指标**：响应时间、吞吐量、slowdown、P99延迟
2. **调度统计**：Task数量、调度开销、阶段分类准确率
3. **动态行为**：Morsel大小变化、优先级变化、状态转换
4. **对比数据**：原生DuckDB vs 各优化阶段 vs 最终系统
5. **扩展性数据**：不同核心数、不同并发度下的性能

请确保实验数据的完整性和准确性，这将直接影响第五章的质量。
